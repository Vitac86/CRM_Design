# Fonts placeholder

В этот каталог для production нужно вручную добавить локальные файлы шрифтов `.woff2` и подключить их через `@font-face` в `../css/crm-static.css`.

Рекомендованный путь:
- `static-uikit/assets/fonts/` (любой удобный подкаталог)
